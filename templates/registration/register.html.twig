{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - S'enregistrer{% endblock %}

{% block body %}

    {{ form_start(registrationForm) }}
    <section class="titre">
        <h1>S'enregistrer</h1>
    </section>

    {{ form_label((registrationForm.email)) }}<br>
    {{ form_widget(registrationForm.email) }}<br>

    {{ form_label(registrationForm.plainPassword, 'Mot de passe') }}<br>
    {{ form_widget(registrationForm.plainPassword) }}<br>

    <button type="submit" class="btn">S'enregistrer</button>
    {{ form_end(registrationForm) }}
{% endblock %}
